
<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Glyph GeoTracker</title>
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css">
  </head>
  <body>

    <header>
      <h1>GeoTracker</h1>
      <h3>Leave a note, let others know where you posted it.</h3>
    </header>
<!-- TODO: identify location in the index.ejs-->
    <form action="/messages" method="POST">
      <input type="text" placeholder="name" name="name">
      <input type="text" placeholder="What's going on?" name="quote">
      <input  hidden id="location" type="text" placeholder="What's going on?" name="locate">
      <button id="find" type="submit">Find my location</button>
      <button type="submit">Submit</button>

    </form>

    <section class="scrollSubmissions">
      <ul class="quotes">
        <% for(var i=0; i<quotes.length; i++) {%>
          <li class="quote" id='<%= quotes[i]._id %>'>
            <span><%= quotes[i].name %></span>
            <!-- this is where the error is  -->
            <span><%= quotes[i].quote %></span>
            <span><%= quotes[i].location %></span>
            <%
              let color = ""

              if(quotes[i].thumbUp){
                color = "red"

              }

             %>
            <i data-name="<%= quotes[i].name %>" data-quotes="<%= quotes[i].quotes %>" class="fas fa-heart <%= color %>" aria-hidden="true"></i>
            <span><i class="fas fa-trash" aria-hidden="true"></i></span>
          </li>
        <% } %>
      </ul>
      <button id="track">Log Glyph Location</button>
      <span id="error-message"></span>
    </section>

    <script type="text/javascript" src="location.js"></script>
  </body>
</html>
